# S 3.7 — independent second pass

Second reading completed 8 September 2026 by an agent independent of the first encoder. Reviewed all 74 extant verses, all first-pass notes, all nine enlarged verse-group images, detailed crops of difficult readings, and the following blank page397. The result is ready for the trial interface; it retains explicit local uncertainty without assigning James a list of transcription tasks.

Source: St. Gallen, Stiftsbibliothek, Cod. Sang.864, [e-codices manuscript](https://www.e-codices.unifr.ch/en/list/one/csg/0864). Evidence is retained in `/tmp/amores-s37`.

## Changes from the first pass

- **3:** Dark `in molli` corrected to `in nullos`, with uncertainty retained only on the dense latter word. The terminal sign is s-like and the paired ascenders support ll. No erased antecedent is invented.
- **39:** The enlarged image resolves `uidi`; removed its first-pass uncertainty.
- **41:** Corrected `possēt/posset` to written `possit`. The i and terminal t are visible; a suspension is not supported.
- **48:** Corrected the supposed barred-e abbreviation to written joined `et` before `fui`.
- **61:** Replaced an overconfident spelled-out `psalter` with an explicitly uncertain abbreviation `psaltꝛ` and tentative expansion. The final sign is present in the scan. The cancellation after `sur` and below-line `a` are recorded as seen; an additional written `das` is no longer manufactured. This preserves evidence without claiming that the entire correction can be reconstructed securely.
- **68:** Preserved the displayed abbreviated `exposcīt`, but marked its inferred expansion low-certainty because the stroke might represent an omitted nasal.

Additional evidence files generated during the second reading include `zoom-39.png`, `zoom-41.png`, `zoom-48.png`, `61-instrument.png`, and `second-61cancel.png`.

## Notes resolved or retained

The names `chide`/`phito` at23, unusual `tacta` at51, `intēpestiua` at67, and `Quid habes` at69 were independently confirmed and retained. These do not require editorial approval simply because other witnesses read differently.

The stains at28–29 and fading after N at27 remain real limitations of the source. Their local `<unclear>` encoding stays; they are recorded uncertainty, not separate human assignments. The dense correction at3, cancellation/abbreviation at61, and expansion at68 remain explicitly tentative. The report does not reclassify unreadable ink as certain merely to reduce a count.

The last written verse on396 is74, `Molliter admota sollicitare manu`. Page397 is blank. The gap for canonical75–84 is confirmed from the images and retained without inferring its physical cause or date.

## Result and checks

The candidate `scripts/output/S-3.7.xml` has74 verse elements,74 linked zones, and the explicit ten-line terminal gap. All verse facsimile links resolve. Full TEI P5 4.12.0 RelaxNG validation passes. There are **zero `type="review"` human-action notes**. Machine-readable dispositions and coverage are in `scripts/output/S-3.7-review.json`.

The trial remains `draft`: an independent agent reading improves the transcription and documents its limits, but does not claim scholarly certification. TEI approaches follow [Primary Sources](https://tei-c.org/release/doc/tei-p5-doc/en/html/PH.html), [Common Elements](https://tei-c.org/release/doc/tei-p5-doc/en/html/CO.html), and the [gap element](https://tei-c.org/release/doc/tei-p5-doc/en/html/ref-gap.html).
