# 3.7 trial — local verification, 8 September 2026

Checked the actual browser at `http://127.0.0.1:4173/` after integrating independent reviews.

- P/Y/O/LL render 84 verse elements; S renders 74 and an explicit absent-ending notice. Hidden metadata and running headers do not leak into the verse display. LL's empty note section is hidden.
- P's four marginal annotations retain their text and uncertainty markup; S's below-line correction is rendered below its cancellation.
- Y's correction finder has three substitution matches. Choosing its first match navigates to line 43 and source image 130; Next moves to line 51. Categories derive from the TEI, omit hidden expansions and distinguish zero encoded erasures from darker ink.
- Source line clicks navigate to the correct ending page: Y 131, S 396 and O 106. The image spotlight corresponds to the selected verse. Viewer witness selection remains independent so scholars can compare different witnesses.
- P's line 23 question opens the correct note; Return to line 23 returns to its verse. P has two question buttons, Y three, S/O/LL none.
- TEI opens the selected witness's XML with a download link. Back to poem restores the poem and witness (checked Y and LL). Minimal toolbar and collapsed companion panel retain usable comparison widths.
- All five extracted TEI documents pass the pinned Relax NG schema and the automated review/coverage/reference checks. The 25 existing site tests pass. JavaScript syntax checks and `git diff --check` pass.
- All content outside 3.7 in P/S/LL is byte-for-byte unchanged from HEAD. Existing annotation data was not edited during this TEI trial.

This records local implementation checks and the separate source-review artifacts. It is not human scholarly approval or a publication record.
